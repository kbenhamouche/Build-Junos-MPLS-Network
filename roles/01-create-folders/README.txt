Role "Create-Folders" has the following purposes:
1- Remove existing host_vars and Create a new one for each node
2- Create a temporary directory for each node in order to merge all generated configurations
3- Remove Create the configuration directory that contains the final configuration to push on each node
